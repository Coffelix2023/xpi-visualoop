## 1. 验证外部接入契约

- [x] 1.1 核对安装版本的 Pi 扩展、工具图像结果、会话生命周期与取消类型，完整阅读相关文档和 Glimpse 接口文档；交付接入记录，列出实际版本、事件签名和采用标准库 spawn 的依据，不以记忆中的 API 代替类型。
- [x] 1.2 使用专用 Chrome 端点和私有 Harness 目录做最小接入验证，确认自有标签页、固定视口与 DPR、page_info、视口截图和图像实际尺寸；记录精确 Harness/Chrome/Python 版本、命令和结果，未通过则停止后续功能实现并修订设计，不自动切换后端。
- [x] 1.3 验证 Harness 标准输入脚本、单个结构化结果、stderr、环境加载、事件观察窗口及取消语义；用私有目录证明不加载项目或用户 helpers、不启用录制/遥测/云启动，记录不支持的诊断范围和实际清理行为。

## 2. 建立受控浏览器上下文

- [x] 2.1 实现用户配置与受信任项目配置解析、本地页面/端点验证和未知字段拒绝；用有效回环地址、外部跳转、用户信息、未受信任项目及恶意程序参数测试验证拒绝路径，确保失败不回退到日常或云浏览器。
- [x] 2.2 实现基于 spawn 的固定脚本适配、数据载荷编码、版本化结果校验、环境白名单、输出预算和超时取消；用可控子进程验证异常退出、额外输出、超量输出、取消及含引号/换行的选择器都不会被当成代码或成功结果。
- [x] 2.3 实现检查上下文、端点所有权占用检测和串行操作，注册显式 `visual_prepare`；在专用浏览器验证 prepare 只创建/导航自有标签页，另一 Pi 进程占用时明确失败，目标被关闭后不切到其他标签页。
- [x] 2.4 将 `/xpi-visualoop` 扩展为状态与 disconnect，并接入会话替换、结束、重载清理；测试取消在途操作、释放所属资源、拒绝旧引用，并验证用户浏览器/profile 和无法安全关闭的页面不被删除或强制关闭。

## 3. 交付视觉证据采集

- [x] 3.1 定义 Capture、Feedback、Comparison 及后端结果的最小类型和运行时校验，建立会话内不可变索引及私有临时文件；测试唯一标识、跨会话拒绝、路径穿越/符号链接拒绝、容量上限及基准不被静默覆盖。
- [x] 3.2 实现 `visual_capture` 的视口图、采集前后元数据、就绪等待与稳定性结果；在页面持续联网、字体或图片未就绪、滚动/导航变化及稳定页面上验证有限时间返回和 degraded/failed 区分，确保不隐式导航、滚动或调整视口。
- [x] 3.3 实现选择器唯一性检查、可见边界、受限文本/无障碍属性和计算样式；测试不存在、多重匹配、视口外、部分可见目标及画布/留白场景，确认无自动滚动和虚构源码映射。
- [x] 3.4 使用同一父截图派生局部图和有界导出图，记录实际图像尺寸、裁切偏移与坐标比例；用 DPR 1/2、非整数缩放和部分可见元素验证往返坐标误差不超过一个导出像素，模型与面板引用同一文件版本。
- [x] 3.5 实现按观察窗口与目标过滤的控制台/网络失败摘要、脱敏和数量限制；用受控错误、请求失败和无法取得历史的场景验证 diagnostics 独立返回、unknown 与零错误不同，输出不包含认证头或请求正文。
- [x] 3.6 注册工具 schema、结果渲染和图像内容输出，完成 prepare/capture 的 Pi 内集成；验证类型与调用参数一致、模型收到实际截图、无界面模式仍可采集、错误和文本均遵守输出预算。

## 4. 交付截图框选与意见回传

- [x] 4.1 实现可选 Glimpse 模块加载和仅含受控静态内容的反馈面板；验证有效截图摘要、缩放/滚动、失效证据错误、动态文本转义、无外部脚本，并按 DESIGN.md 与 Glimpse 设计规范进行实际窗口检查。
- [x] 4.2 实现单矩形选择、反向拖动、图像留白处理、区域数字输入和文字校验；用坐标测试验证缩放及滚动后的原图区域，实际键盘验证焦点、区域调整、多行文字、显式提交及 Esc 取消。
- [x] 4.3 注册 `visual_feedback` 并通过原工具结果交付 submitted/cancelled，实施面板消息验证和只完成一次规则；测试双击、重复消息、错误截图标识、非法坐标、空意见和超长意见，确认只交付一次且不额外调用 sendUserMessage。
- [x] 4.4 实现面板 busy、超时、关闭、工具取消、会话切换和重载失效；测试迟到提交不进入新会话，等待和窗口被释放，第二次面板请求不覆盖正在编辑的意见。
- [x] 4.5 实现无 Glimpse 的 Pi 整图文字反馈降级及无对话模式的 unavailable；分别验证路径/引用、整图范围提示、提交/取消区分，以及采集功能不受窗口加载失败影响。

## 5. 交付修改后复核

- [x] 5.1 实现 `visual_verify` 基准解析、新证据生成及条件比较，继承基准目标而不重放页面操作；测试过期基准、跨上下文、视口/DPR/路由/滚动/声明状态变化及资源不稳定，确认 not-comparable 包含具体原因且不覆盖基准。
- [x] 5.2 实现目标边界/样式变化摘要、共同视口区域裁切和诊断分项展示；用目标移动、变大、字体改变及目标丢失测试验证预期布局变化不会独自导致不可比较，无法取得新图时保留失败原因且不打开空白比较面板。
- [x] 5.3 在 `visual_feedback` 中增加 comparisonId 模式、前后并排图、明确认可及对 after 图继续反馈；验证 accepted、submitted、cancelled 三种结果绑定正确版本，采集成功或图像变化均不自动生成设计通过结论。

## 6. 完成闭环验证与使用交付

- [x] 6.1 在真实本地页面走通 prepare、capture、框选反馈、人工/Agent 修改、verify、用户认可的完整链路，并覆盖滚动目标、DPR 2、过期窗口、断开端点和清理；记录截图引用与实际结果，不把未执行的浏览器或用户窗口检查记为通过。
- [x] 6.2 更新双语 README、配置/专用 Chrome 设置说明和必要术语，列明四个工具、状态/disconnect、预算、降级、临时文件与 Pi/模型副本边界、已验证版本/平台及回滚方法；按说明复走安装与调用路径，macOS/Fedora 未验证部分明确标注。
- [x] 6.3 对实现运行语言服务诊断，并分别执行 `pnpm typecheck`、`pnpm -w run lint`、`pnpm test`，确认所有必需检查通过；核对三份 spec 的验收场景均有自动测试或真实运行证据，未完成项保持未勾选，不因文档齐备宣称实现完成。
